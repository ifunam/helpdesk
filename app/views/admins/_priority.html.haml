.prioriy{:id => priority.dom_id }
  .priority_name
    = priority.name
  .priority_action
    = link_to_remote 'Eliminar', :url => {:action => :destroy, :type => :priority, :id => priority.id}, :method => :delete