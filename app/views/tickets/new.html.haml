#ticket_form
  %h3 Solicitud de Soporte
  - form_for @ticket, :url => tickets_url do |f|
    %p
      %b
        = f.label "Reportar problemas con :"
      = f.select :category_id, Category.all(:order => 'name ASC').collect { |record|[record.name, record.id] }
    %p
      %b
        = f.label "Descripción:"
      = f.text_area :body, :size => "80x10"
    %p
      %b
        = f.label "Ubicacion del reporte"
      = f.text_area :location_report, :size => "80x2"
    %p
      %b
        = f.label "Ubicacion del equipo"
      = f.text_area :location_problem, :size => "80x2"
    #button_new
      = submit_tag "Enviar Solicitud"
  #cancel
    = button_to 'Cancelar',tickets_url, :method => :get
