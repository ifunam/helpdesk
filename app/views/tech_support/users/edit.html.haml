#edit
  - form_for(:user, @user, :url => {:action => 'update'}, :html => {:method => :put}) do |f|
    = render :partial => 'form', :locals => { :f => f}
    = submit_tag('Guardar cambios')
  #cancel
    = button_to "Cancelar", {:controller => @user.class.name.pluralize}, :method => :get
