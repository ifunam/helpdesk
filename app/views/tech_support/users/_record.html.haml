.record{:id => "color#{record.id%2}"}
  .record_item#name
    = record.login
  .record_item#actions
    .link
      = link_to "Mostrar", {:action => 'show',:id => record.id}
    .link
      = link_to "Modificar", {:action => 'edit', :id => record.id}
    .link
      = link_to "Borrar", {:action => 'destroy',:id => record.id}, :method => :delete, :confirm => "¿Deseas borrar este registro?"
