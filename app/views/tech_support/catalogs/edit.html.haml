- form_for(:record, @record, :url => {:action => 'update'}, :html => {:method => :put}) do |f|
  = render :partial => 'tech_support/catalogs/form', :locals => { :f => f}
  %br
  = submit_tag('Guardar cambios')